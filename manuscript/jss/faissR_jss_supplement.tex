\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage[T1]{fontenc}
\usepackage{lmodern}

\definecolor{faissblue}{HTML}{1F4D78}
\pagestyle{fancy}
\fancyhf{}
\lhead{\textit{faissR} supplementary material}
\rhead{\thepage}
\setlength{\headheight}{14pt}
\setlength{\emergencystretch}{3em}
\renewcommand{\headrulewidth}{0.4pt}

\newcommand{\code}[1]{\texttt{#1}}
\newcommand{\pkg}[1]{\textsf{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\title{\textbf{Supplementary Material}\\[0.4em]
\large \pkg{faissR}: CPU and GPU Nearest-Neighbor Search with FAISS and cuVS in R}
\author{Moussa Kassim$^{1,2,\dagger}$, Martin Ocharo$^{1,2,\dagger}$\\
Dalia Ahmed$^{1}$, Dupe Ojo$^{1}$\\
Alessia Vignoli$^{3,4}$, Leonardo Tenori$^{3,4}$\\
Dinesh Gupta$^{5}$, Silvano Piazza$^{6,7}$\\
Stefano Cacciatore$^{1,2,\ast}$}
\date{28 August 2026}

\begin{document}
\raggedbottom
\maketitle

\noindent
$^{1}$ Bioinformatics Unit, International Centre for Genetic Engineering and
Biotechnology (ICGEB), Cape Town 7925, South Africa.\\
$^{2}$ Department of Integrative Biomedical Sciences, Institute of Infectious
Disease \& Molecular Medicine (IDM), University of Cape Town, Cape Town 7925,
South Africa.\\
$^{3}$ Department of Chemistry ``Ugo Schiff'', University of Florence, Sesto
Fiorentino, Italy.\\
$^{4}$ Magnetic Resonance Center (CERM), University of Florence, Sesto
Fiorentino, Italy.\\
$^{5}$ Translational Bioinformatics Group, International Centre for Genetic
Engineering and Biotechnology (ICGEB), New Delhi, India.\\
$^{6}$ Computational Biology Group, International Centre for Genetic
Engineering and Biotechnology (ICGEB), Trieste, Italy.\\
$^{7}$ Bioinformatics Facility, Department of Cellular, Computational and
Integrative Biology (CIBIO), University of Trento, Trento, Italy.\\[0.5em]
\textsuperscript{$\dagger$}Moussa Kassim and Martin Ocharo contributed equally.\\
\textsuperscript{*}Corresponding author: Stefano Cacciatore,
\href{mailto:stefano.cacciatore@icgeb.org}{stefano.cacciatore@icgeb.org}.

\section{Scope}

This supplement records the minimum additional information required to
interpret and reproduce the results in the article. It covers the software
environment, dataset dimensions, calibration grids, exactness and target
criteria, completed evidence, and aggregation rules. It does not report
unanalysed jobs or development-stage results.

The evaluated nearest-neighbor implementation was \pkg{faissR} 0.99.21 at
commit:
\begin{center}
\texttt{0903532baf02b340a90921db18edc4deae5ea462}
\end{center}
The evaluation included only the three public metrics Euclidean, cosine, and correlation. All submitted
results are linked to the package commit and Singularity-image fingerprint.

\section{Software and hardware}

\begin{table}[h]
\centering
\caption{Computational environment.}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.42\textwidth}Y@{}}
\toprule
Component & Configuration \\
\midrule
Operating system & Debian GNU/Linux 13 (trixie) in Singularity \\
R & 4.5.3, x86\_64-conda-linux-gnu \\
Linear algebra & OpenBLAS 0.3.33; LAPACK 3.12.0 \\
CPU execution & UCT ada partition; 12 requested threads per job \\
GPU execution & NVIDIA L40S, 46,068 MiB, compute capability 8.9 \\
NVIDIA driver & 595.58.03 \\
Input representation & float32 for all publication benchmark rows \\
External CPU comparator & \pkg{BiocNeighbors} 2.4.0 \\
\bottomrule
\end{tabularx}
\end{table}

The ada partition contains several processor generations. Consequently, CPU
results are paired within compatible cells and summarized by dataset; no single
processor model is assigned to all CPU jobs. CUDA timing ends only after device
synchronization. The L40S-derived automatic policy remains callable elsewhere,
but its metadata identify it as calibration evidence rather than a universal
hardware optimum.

\section{Datasets and references}

The real-data collection comprised COIL20 (1,440 by 16,384), FashionMNIST
(70,000 by 784), flow18 (1,000,021 by 11), FR-FCM-ZYRM (5,220,347 by 32),
ImageNet features (1,281,167 by 1,024), mass41 (965,282 by 14), MetRef (873 by
375), MNIST (70,000 by 784), and USPS (11,000 by 256). Labels were not used by
nearest-neighbor timing or recall calculations.

COIL20 was flattened from processed grayscale images; FashionMNIST and MNIST
combined and flattened the official training and test IDX files; and USPS was
loaded from the recorded \pkg{Rdimtools} release. FR-FCM-ZYRM retained 32
biological-marker channels from 102 FCS files and used
$\operatorname{asinh}(x/5)$. flow18 and mass41 used the supplied opt-SNE
matrices without further transformation. ImageNet consisted of
1,024-dimensional DINOv2 features from ILSVRC 2012 training images. MetRef
removed zero-sum variables and used KODAMA normalization and scaling. The
corresponding source citations are \cite{Nene1996,Xiao2017,LeCun1998,
YouShung2022,VanUnen2017,Belkina2019,Russakovsky2015,Oquab2024,
Cacciatore2014}; the replication provenance manifest records source URLs,
releases, accessions, terms, fingerprints, and complete conversion text for
each dataset.

Exact references used 1,024 sampled queries and stored the first 100 neighbors.
The same reference was cropped for $k=15,30,50,100$. The audited archive
contained 87 reference records for the three public metrics, all successful and
unique, and passed an independent CPU cross-audit. Exact-family results were accepted when neighbor
identifiers agreed or when sorted boundary distances agreed within the frozen
float32 tolerance. This tie-aware rule prevents arbitrary tied-neighbor choices
from being reported as ANN recall failures.

The replication material contains acquisition and preprocessing instructions,
source identifiers, and object fingerprints. Converted datasets are not
redistributed when their source terms do not clearly permit redistribution;
the scripts retrieve the original source instead.

\section{Calibration design}

Calibration crossed metric, backend, $k\in\{15,30,50,100\}$, and target
$\tau\in\{0.90,0.95,0.99\}$. Table~\ref{tab:grid} gives the parameter families.
Candidate values were repaired when required by an implementation constraint,
for example a valid product-quantizer dimension. Failed, timed-out, and
out-of-memory candidates were retained as negative evidence and were never
eligible for selection.

\begin{table}[h]
\centering
\caption{Method-specific calibration parameters.}
\label{tab:grid}
\begin{tabularx}{\textwidth}{@{}lY@{}}
\toprule
Method & Tuned quantities \\
\midrule
Exhaustive Flat/brute force & Query batch size, threads, resource reuse \\
Grid & Cell width and search expansion \\
HNSW & Graph degree, construction effort, search effort \\
IVF & Number of lists and probed lists \\
IVF-PQ & IVF controls, subquantizers, code width, query batch \\
IVF-PQ FastScan & IVF-PQ controls, block size, optional refinement \\
CAGRA & Graph and intermediate degree, build strategy, search width \\
faissR NN-descent-derived refinement & Candidate pool, graph degree, refinement iterations \\
faissR NSG-/Vamana-derived refinements & Seed graph size, output degree, search breadth \\
\bottomrule
\end{tabularx}
\end{table}

For approximate calibration candidate $s$, eligibility requires mean query
recall $R_s\geq\tau$. Each candidate was timed once and the eligible candidate
with minimum elapsed time was selected, with deterministic tie-breaking by
higher recall and candidate identifier. During independent validation, target
attainment requires mean query recall $R_{sr}\geq\tau$ in every prespecified
replicate $r$. Minimum query recall is diagnostic and does not define
attainment. Exact-audited candidates form a separate eligibility class.

For the three public metrics, 6,453 of 6,804 planned calibration operating
points produced a completed recommendation. The 351 unavailable points were
318 CPU and 33 CUDA cells and were excluded rather than imputed. Among completed
points, 1,728 were exact-audited. Of 4,725 approximate points, 3,653 met the
target and 1,072 did not. The latter remain visible in policy evidence as
\code{best\_recall\_below\_target} and cannot be described as target attaining.

\begin{table}[h]
\centering
\caption{Unavailable calibration operating points by backend, method, and
recorded failure class.}
\label{tab:calibration-missing}
\begin{tabular}{@{}llrr@{}}
\toprule
Backend & Method & Time limit & Memory kill \\
\midrule
CPU & Brute force & 72 & 0 \\
CPU & Exact & 72 & 0 \\
CPU & Flat & 72 & 0 \\
CPU & IVF & 12 & 12 \\
CPU & NSG-derived refinement & 42 & 0 \\
CPU & Vamana-derived refinement & 36 & 0 \\
CUDA & NN-descent-derived refinement & 0 & 9 \\
CUDA & NSG-derived refinement & 0 & 12 \\
CUDA & Vamana-derived refinement & 0 & 12 \\
\midrule
Total & & 306 & 45 \\
\bottomrule
\end{tabular}
\end{table}

By metric, the unavailable totals were 117 correlation, 123 cosine, and 111
Euclidean points. By dataset, they were 201 FR-FCM-ZYRM, 144 ImageNet-feature,
and 6 flow18 points. The detailed machine-readable table crosses backend,
method, metric, dataset, and reason; no missing point was imputed.

\begin{table}[h]
\centering
\caption{Selection sensitivity from eligible-candidate timing margins. The
margin is $T_{(2)}/T_{(1)}-1$, where $T_{(1)}$ and $T_{(2)}$ are the fastest and
second-fastest eligible times. These are near-tie diagnostics, not repeated
timing estimates.}
\label{tab:calibration-stability}
\begin{tabular}{@{}lrrr@{}}
\toprule
Comparison & Points & Median margin & Within 5\% \\
\midrule
Configuration, all & 5,285 & 2.67\% & 60.8\% \\
Configuration, CPU & 2,462 & 6.71\% & 43.6\% \\
Configuration, CUDA & 2,823 & 0.73\% & 75.8\% \\
Method family, all & 641 & 50.24\% & 10.1\% \\
\bottomrule
\end{tabular}
\end{table}

The raw campaign contains one successful row per candidate key, so a frequency
of winner changes across calibration timing replicates is not identifiable.
The near-tie fractions quantify how often modest timing noise could change a
configuration or method-family choice. At the configuration level this
sensitivity was greatest on CUDA, whereas distinct method families were usually
well separated.

\begin{table}[h]
\centering
\caption{Calibration-to-validation change. Recall change is validation mean
recall minus calibration recall; time ratio is median validation time divided by
calibration time.}
\label{tab:calibration-validation}
\begin{tabular}{@{}llrrr@{}}
\toprule
Backend & Route & Points & Recall change & Time ratio \\
\midrule
CPU & HNSW & 324 & $+0.0053$ & 1.05 \\
CUDA & Automatic, all & 324 & $0.0000$ & 0.96 \\
CUDA & Automatic, Flat & 280 & $0.0000$ & 0.95 \\
CUDA & Automatic, IVF & 44 & $+0.0096$ & 2.12 \\
\bottomrule
\end{tabular}
\end{table}

\section{Validation and aggregation}

Validation used two query seeds and three timing repeats for each dataset,
metric, $k$, and target. Calibration and validation query samples were
independent, but both were drawn from the same named dataset. A validation cell
passed only when every replicate completed and every replicate's mean recall
met the target. Exact-audited cells were not reclassified by identifier-overlap
recall.

A separate CUDA leave-one-dataset-out analysis removed every row associated
with the held-out named dataset before selecting a method family. It used the
same prespecified shape group when available and otherwise the three nearest
training datasets in $\log(n)$--$\log(p)$ space. The held-out dataset was used
only for evaluation. The package's compiled \texttt{auto} result was reported
separately because that policy summarizes the full calibration collection and
is therefore not independent LOODO evidence. CPU \texttt{auto} was not
evaluated in the frozen campaign; CPU HNSW results must not be interpreted as
CPU selector validation.

The complete held-out CUDA set included automatic selection, explicit exact
Flat, explicit brute force, and CAGRA for all nine datasets and three metrics.
Each route contributed 1,944 successful rows. The complete CPU faissR HNSW set
contained 1,944 successful rows. The separate-job external HNSW archive covered
Euclidean and cosine distance on seven datasets. It is retained for execution
accounting but is not used for primary CPU speed ratios because routes were not
coexecuted on the same node.

The controlled CPU HNSW experiment fixes dataset, $k$, and comparator within a
single Slurm array task. \pkg{faissR} and either \pkg{BiocNeighbors} or
\pkg{RcppHNSW} run on the same node and allocation. The initial route is
randomized reproducibly within each validation seed and alternated over five
repetitions. Each route repetition runs in an isolated worker after an untimed
128-row warm-up. Both routes receive the same double matrix. The experiment
reports end-to-end cold-call time, reusable-index build time, and fitted-index
query time separately. For every phase, the ratio is
$T_{\mathrm{comparator}}/T_{\mathrm{faissR}}$; values above one favor
\pkg{faissR}. The audit retains failures and timeouts and rejects any pair that
does not verify a common hostname, allocation, and opposing route positions.
Recall-equivalent summaries require both routes to attain mean recall at least
0.99 in each included validation replicate.

\section{Result audit}

\begin{table}[h]
\centering
\caption{Evidence used by the article.}
\begin{tabularx}{\textwidth}{@{}Yrr@{}}
\toprule
Evidence & Passing or completed & Total \\
\midrule
CPU route-contract cells & 48 & 48 \\
CUDA route-contract cells & 52 & 52 \\
Public-metric reference records & 87 & 87 \\
Completed calibration operating points & 6,453 & 6,804 planned \\
Approximate calibration targets met & 3,653 & 4,725 completed \\
CUDA automatic held-out cells & 324 & 324 \\
CPU HNSW held-out cells & 304 & 324 \\
CUDA LOODO operating-point cells & 324 & 324 \\
CUDA LOODO method-family agreement & 300 & 324 \\
\bottomrule
\end{tabularx}
\end{table}

The CUDA selector chose Flat in 280 held-out cells and IVF in 44 Euclidean
cells. All IVF-selected cells met the target in every replicate. Dataset-level
paired speed ratios (first route favored when greater than one) were 0.99 for
automatic versus explicit exact, 0.87 versus brute force, and 2.23 versus
CAGRA. In IVF-selected cells the median ratio versus exact was 4.59 across four
datasets, with a range of 0.14--5.83. CPU provider speed ratios are withheld
until the controlled same-node HNSW audit is complete; the separate-job archive
is not used to estimate a software speedup.

\begin{table}[ht]
\centering
\small
\caption{CUDA results by dataset. Automatic target attainment covers 36
metric--$k$--target cells. All routes completed without timeout. Ratios use
second-route time divided by automatic-route time. The last ratio is restricted
to cells in which automatic selection chose IVF; values above one favor IVF.
Ratio entries are dataset medians of matched replicate ratios.}
\label{tab:supp-cuda-dataset}
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
Dataset & Auto target & Exact/auto & Brute/auto & CAGRA/auto & IVF target & Exact/IVF \\
\midrule
COIL20 & 36/36 & 0.98 & 0.87 & 1.00 & 0/0 & -- \\
FashionMNIST & 36/36 & 0.98 & 1.18 & 10.94 & 0/0 & -- \\
FR-FCM-ZYRM & 36/36 & 1.00 & 1.00 & 0.81 & 12/12 & 3.69 \\
MNIST & 36/36 & 0.99 & 1.18 & 10.96 & 0/0 & -- \\
MetRef & 36/36 & 0.94 & 0.70 & 2.40 & 0/0 & -- \\
USPS & 36/36 & 0.96 & 0.75 & 2.58 & 0/0 & -- \\
flow18 & 36/36 & 1.00 & 0.83 & 0.96 & 12/12 & 5.83 \\
ImageNet features & 36/36 & 1.01 & 1.90 & 2.23 & 8/8 & 0.14 \\
mass41 & 36/36 & 1.00 & 0.85 & 1.00 & 12/12 & 5.49 \\
\bottomrule
\end{tabular}
\end{table}

For LOODO, the cross-fitted selector abstained in 0/324 cells, selected an
exact-audited family in 288/324, and selected CAGRA in 36/324. Its median
elapsed-time ratio to the held-out empirical oracle was 1.00 (90th percentile
1.00; range 1.00--2.24). This is evidence of within-collection
dataset-withholding robustness, not cross-collection or hardware-independent
generalization.

\section{CUDA automatic-selection decomposition}

The 324 CUDA automatic cells comprised 280 Flat and 44 IVF selections. By
metric, Flat/IVF counts were 64/44 for Euclidean, 108/0 for cosine, and 108/0
for correlation. By target, the counts were 92/16 at 0.90, 92/16 at 0.95, and
96/12 at 0.99. For each of $k=15,30,50,100$, the counts were 70/11 among 81
cells.

\begin{table}[h]
\centering
\caption{CUDA automatic route selections by dataset. Each row contains 36
metric--$k$--target cells.}
\begin{tabularx}{\textwidth}{@{}Xrr@{}}
\toprule
Dataset & Flat & IVF \\
\midrule
COIL20 & 36 & 0 \\
FashionMNIST & 36 & 0 \\
FR-FCM-ZYRM & 24 & 12 \\
flow18 & 24 & 12 \\
ImageNet features & 28 & 8 \\
mass41 & 24 & 12 \\
MetRef & 36 & 0 \\
MNIST & 36 & 0 \\
USPS & 36 & 0 \\
\bottomrule
\end{tabularx}
\end{table}

Table~\ref{tab:auto-ivf-parameters} records the frozen IVF parameters for the
44 approximate selections. The three large low-dimensional datasets were
FR-FCM-ZYRM, flow18, and mass41. ImageNet features supplied the large
high-dimensional case. Numeric parameters changed with target for the large
low-dimensional $k=15$ and $k=30$ strata. At $k=50$ and $k=100$, target labels
changed but the numeric IVF setting did not. ImageNet changed route rather than
IVF parameters at target 0.99.

\begin{table}[h]
\centering
\caption{Frozen CUDA IVF decisions as $(\code{nlist},\code{nprobe})$.
``Flat'' indicates a target-dependent route change.}
\label{tab:auto-ivf-parameters}
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
Shape represented in evaluation & $k$ & 0.90 & 0.95 & 0.99 \\
\midrule
Large low-dimensional & 15 & (512, 6) & (512, 6) & (512, 12) \\
Large low-dimensional & 30 & (512, 6) & (1024, 16) & (1024, 32) \\
Large low-dimensional & 50 & (1966, 90) & (1966, 90) & (1966, 90) \\
Large low-dimensional & 100 & (5898, 308) & (5898, 308) & (5898, 308) \\
ImageNet features & 15 & (1590, 160) & (1590, 160) & Flat \\
ImageNet features & 30 & (1060, 99) & (1060, 99) & Flat \\
ImageNet features & 50 & (1590, 160) & (1590, 160) & Flat \\
ImageNet features & 100 & (1590, 160) & (1590, 160) & Flat \\
\bottomrule
\end{tabularx}
\end{table}

Across the 108 dataset--metric--$k$ strata, target recall changed method in 4,
changed numeric parameters without changing method in 6, and changed neither
in 98. These frequencies delimit the empirical contribution of recall-aware
selection: it adapts the evaluated large Euclidean cases, while exhaustive
search remains the stable choice elsewhere.

\section{Replication}

The submission material contains:
\begin{itemize}
\item the installable \pkg{faissR} source package and reference manual;
\item a package vignette and the executable practical CPU example;
\item a single commented article replication script and its HTML output;
\item a frozen result archive, its SHA-256 ledger, and a pre-analysis checksum
  gate;
\item machine-readable reconstructions of all 15 article and supplementary
  tables, with a table checksum manifest;
\item software, container, dataset, and result fingerprints; and
\item a reduced CPU path that reproduces the interface example without a GPU.
\end{itemize}
The full CUDA evaluation requires a compatible NVIDIA GPU and the recorded
FAISS/cuVS build. Reanalysis of the frozen output does not require a GPU. The
standalone script has three explicit modes: \code{compact} runs interface
examples on an ordinary CPU computer, \code{archive} verifies the frozen
archive before extraction and recreates every reported table, and \code{all}
runs both. Any digest, schema, dataset-fingerprint, aggregation, or table-audit
failure stops execution. A successful run records archive verification,
per-table SHA-256 digests, and a fresh \code{sessionInfo.txt}.

\section{Interpretation}

The evaluation establishes the package contracts and characterizes
calibration-informed operating points on the tested data and hardware. The
returned evidence metadata makes the selected provider, parameters, and recall
status inspectable. When application-specific recall is critical, users can
additionally validate the selected route against an exact query subsample.

\bibliographystyle{plain}
\bibliography{faissR_jss}

\end{document}
