\documentclass[article]{jss}

\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{lmodern}

\hypersetup{hypertexnames=false}
\setlength{\emergencystretch}{2em}

\newcommand{\class}[1]{\code{#1}}
\newcommand{\fct}[1]{\code{#1()}}
\newcommand{\method}[1]{\code{#1}}

\author{Moussa Kassim$^{1,2,\dagger}$
  \And Martin Ocharo$^{1,2,\dagger}$
  \AND Dalia Ahmed$^{1}$
  \And Dupe Ojo$^{1}$
  \AND Alessia Vignoli$^{3,4}$
  \And Leonardo Tenori$^{3,4}$
  \AND Dinesh Gupta$^{5}$
  \And Silvano Piazza$^{6,7}$
  \AND Stefano Cacciatore$^{1,2,\ast}$}
\Plainauthor{Moussa Kassim, Martin Ocharo, Dalia Ahmed, Dupe Ojo, Alessia Vignoli, Leonardo Tenori, Dinesh Gupta, Silvano Piazza, Stefano Cacciatore}

\title{\pkg{faissR}: CPU and GPU Nearest-Neighbor Search with FAISS and cuVS in \proglang{R}}
\Plaintitle{faissR: CPU and GPU Nearest-Neighbor Search with FAISS and cuVS in R}
\Shorttitle{FAISS and cuVS Nearest-Neighbor Search in \proglang{R}}

\Abstract{
Nearest-neighbor search is a computational primitive for statistical learning,
manifold methods, retrieval, and local prediction. Efficient use from
\proglang{R} requires more than exposing an algorithm: data representation,
metric transformations, index reuse, device transfers, and the choice of an
accuracy--time operating point all affect performance. \pkg{faissR} is an
open-source, Python-free interface to the FAISS \proglang{C++} library and,
optionally, NVIDIA CUDA and RAPIDS cuVS. A common interface provides exact,
inverted-file, product-quantization, graph, and low-dimensional searches for
Euclidean, cosine, and correlation distance. The package accepts double and
\pkg{float} matrices, supports reusable fitted indexes, performs batching and
self-neighbor removal in compiled code, and can return nearest-neighbor buffers
that remain on an NVIDIA GPU. Recall-aware policies use benchmarked operating
points to select a method and its parameters from data shape, metric, number of
neighbors, backend, and a requested recall tier of 0.90, 0.95, or 0.99, while
exposing the selected route and supporting evidence as result metadata. In a
nine-dataset evaluation, all 100 CPU/CUDA route-contract tests passed. The
CUDA automatic selector met its
replicate-level criterion in all 324 held-out cells, selecting exhaustive Flat
search in 280 cells and IVF in 44. In a separate leave-one-dataset-out analysis,
cross-fitted method selection attained the requested operating point in all 324
cells and agreed with the held-out empirical oracle in 300. CPU evidence is
reported for HNSW and is not presented as validation of CPU automatic selection.
We describe the statistical contract,
software design, practical interface, calibration procedure, and reproducible
evaluation of \pkg{faissR}.
}

\Keywords{nearest neighbors, approximate search, FAISS, CUDA, float32, \proglang{R}}
\Plainkeywords{nearest neighbors, approximate search, FAISS, CUDA, float32, R}

\Address{
  \textsuperscript{1}Bioinformatics Unit\\
  International Centre for Genetic Engineering and Biotechnology (ICGEB)\\
  Cape Town 7925, South Africa\\
  \textsuperscript{2}Department of Integrative Biomedical Sciences, Institute
  of Infectious Disease \& Molecular Medicine (IDM), University of Cape Town\\
  Cape Town 7925, South Africa\\
  \textsuperscript{3}Department of Chemistry ``Ugo Schiff'', University of
  Florence, Sesto Fiorentino, Italy\\
  \textsuperscript{4}Magnetic Resonance Center (CERM), University of Florence,
  Sesto Fiorentino, Italy\\
  \textsuperscript{5}Translational Bioinformatics Group, International Centre
  for Genetic Engineering and Biotechnology (ICGEB), New Delhi, India\\
  \textsuperscript{6}Computational Biology Group, International Centre for
  Genetic Engineering and Biotechnology (ICGEB), Trieste, Italy\\
  \textsuperscript{7}Bioinformatics Facility, Department of Cellular,
  Computational and Integrative Biology (CIBIO), University of Trento,
  Trento, Italy\\[1ex]
  \textsuperscript{\(\dagger\)}Moussa Kassim and Martin Ocharo contributed
  equally and share first authorship.\\
  \textsuperscript{*}Corresponding author:\\
  Stefano Cacciatore, \email{stefano.cacciatore@icgeb.org}\\
  ORCID: \url{https://orcid.org/0000-0001-7052-7156}\\
  Project: \url{https://github.com/tkcaccia/faissR}
}

\begin{document}

\section[Introduction]{Introduction} \label{sec:introduction}

For a reference matrix $X \in \mathbb{R}^{n \times p}$ and query matrix
$Q \in \mathbb{R}^{m \times p}$, nearest-neighbor search returns the $k$
reference rows closest to each query. The operation underlies local regression
and classification, clustering, anomaly detection, manifold learning, and
exploration of high-throughput biological measurements. Exhaustive self-search
has quadratic distance-evaluation cost, whereas approximate nearest-neighbor
(ANN) methods exchange some accuracy for lower time or memory.

Several compiled nearest-neighbor interfaces are available in the
\proglang{R} ecosystem. \fct{RANN::nn2} exposes exact or error-bounded search
with ANN k-d and box-decomposition trees, while \fct{FNN::get.knn} and
\fct{FNN::get.knnx} provide k-d-tree, cover-tree, cosine-oriented CR, and
linear brute-force searches \citep{RANN,FNN}. \fct{nabor::knn} wraps libnabo
k-d trees and the \class{WKNNF}/\class{WKNND} classes retain a tree for repeated
queries; \fct{Rnanoflann::nn} provides a nanoflann-based alternative
\citep{nabor,Rnanoflann}. Approximate-search interfaces include the Annoy index
classes in \pkg{RcppAnnoy}; \fct{hnsw\_knn}, \fct{hnsw\_build}, and
\fct{hnsw\_search} in \pkg{RcppHNSW}; and \fct{nnd\_knn},
\fct{rnnd\_build}, and \fct{rnnd\_query} in \pkg{rnndescent}
\citep{RcppAnnoy,RcppHNSW,rnndescent}. Within Bioconductor,
\pkg{BiocNeighbors} provides \fct{findKNN}, \fct{queryKNN}, and
\fct{buildIndex} as a common interface to exhaustive, k-means kNN,
vantage-point-tree, Annoy, and HNSW implementations \citep{BiocNeighbors}.
These packages establish a rich CPU-oriented R ecosystem, and \pkg{faissR} is
intended to complement rather than replace them. Its added value is the
integration, in one native \proglang{R} contract, of the broader FAISS index
family with optional cuVS execution and GPU-resident result buffers.
The comparator interfaces surveyed here return host-resident \proglang{R}
objects; none exposes a nearest-neighbor result that remains resident in
NVIDIA device memory for direct use by downstream compiled \proglang{R}
packages. Consequently, CPU algorithm families can be compared through their
public interfaces, whereas there is no like-for-like external
GPU-resident-\proglang{R} comparator for \fct{nn\_gpu}.

FAISS implements mature exhaustive, inverted-file, product-quantization, and
graph indexes on CPUs and NVIDIA GPUs \citep{JohnsonDouzeJegou2021,DouzeEtAl2024}.
RAPIDS cuVS provides CUDA vector-search primitives and is integrated with
recent FAISS builds \citep{cuVS,FaissCuVS}. A statistically usable
\proglang{R} interface must additionally reconcile column-major double matrices
with predominantly row-major float32 libraries, define consistent metric and
ordering semantics, avoid rebuilding indexes for repeated queries, and make
device transfers explicit.

\pkg{faissR} addresses these integration problems. It is not a new ANN
algorithm; it is a common execution and evidence layer for FAISS CPU, optional
FAISS GPU, and optional direct CUDA/cuVS providers. Relative to existing
\proglang{R} nearest-neighbor interfaces, the package combines:
\begin{itemize}
\item one metric, ordering, and result contract across exhaustive Flat,
inverted-file, product-quantization, HNSW, CAGRA, and graph-refinement routes;
\item direct \pkg{float} input and compiled metric transformation, batching,
sorting, and self-neighbor removal, reducing conversion and R-side processing;
\item reusable fitted indexes and GPU resources, plus owning GPU-resident
nearest-neighbor buffers that downstream packages can consume through a public
C-callable interface without a mandatory device-to-host result copy; and
\item recall-targeted method and parameter selection with inspectable metadata
for the requested and resolved route, backend, data conversion, result
residency, device transfers, and calibration-hardware provenance.
\end{itemize}
The distinguishing contribution is this combination of FAISS/cuVS method
breadth, representation-aware execution, reusable CPU/GPU state, and auditable
automatic selection through one \proglang{R} API.
The remainder of the article defines these contracts, illustrates their use,
and evaluates correctness, recall attainment, and paired execution time.

\section{Statistical and software design} \label{sec:design}

\subsection{Distances and recall}

The public metrics are Euclidean distance,
\begin{equation}
d_E(x,q)=\left\|x-q\right\|_2,
\end{equation}
cosine distance,
\begin{equation}
d_C(x,q)=1-\frac{x^\top q}{\|x\|_2\|q\|_2},
\end{equation}
and correlation distance,
\begin{equation}
d_R(x,q)=1-\frac{(x-\bar{x}\mathbf{1})^\top
(q-\bar{q}\mathbf{1})}{\|x-\bar{x}\mathbf{1}\|_2
\|q-\bar{q}\mathbf{1}\|_2}.
\end{equation}
Cosine search therefore uses row normalization, while correlation search first
centers each row and then normalizes it. Cosine distance is undefined for an
all-zero vector, and correlation distance is undefined for a constant row.
For CPU routes, \pkg{faissR} applies an explicit computational convention to
these zero-normalized rows: two such rows have distance 0, whereas a
zero-normalized row and a nonzero row have distance 1. CPU Flat search invokes
the exact scorer for affected rows to preserve deterministic boundary-tie
handling. Explicit CUDA routes instead reject zero-vector cosine input and
constant-row correlation input with an informative error; they never copy the
data to the CPU for silent repair. These values are documented software
conventions for otherwise undefined cases, not mathematical cosine or Pearson
correlations. Every result records \code{distance\_is\_metric},
\code{distance\_semantics}, \code{distance\_comparable\_across\_queries}, and
\code{distance\_order}. These fields identify the metric contract, but the
returned object does not carry a per-row mask of zero-vector or constant-row
inputs; callers that need row-level provenance should retain that mask before
search. CUDA errors identify the unsupported condition before returning a
result.

The public metric vocabulary is deliberately restricted to
\code{"euclidean"}, \code{"cosine"}, and \code{"correlation"}. Legacy aliases
and other distances are rejected before provider dispatch. Method, backend, and
metric support is also checked as one contract: an explicit unsupported
combination fails without changing the metric, method, or device.
\code{nn\_capabilities(runtime = TRUE)} exposes this matrix together with local
FAISS, CUDA, and cuVS availability, allowing applications and benchmark scripts
to preflight a request.

For exact neighbor identifiers $N_i^{\star}(k)$ and candidate identifiers
$N_i(k)$, query-level recall is
\begin{equation}
r_i(k)=\frac{|N_i(k)\cap N_i^{\star}(k)|}{k},
\qquad
\bar r(k)=\frac{1}{m}\sum_{i=1}^{m}r_i(k).
\label{eq:recall}
\end{equation}
For an approximate route, a validation cell attains target $\tau$ only when
$\bar r(k)\geq\tau$ in every prespecified validation replicate. Median and
minimum query-level recall are reported as diagnostics but do not define target
attainment. Exhaustive routes are assessed separately by a tie-aware exactness
audit: equality of identifiers is accepted directly, while differences at a
tied $k$th boundary are accepted when sorted distances agree within the frozen
float32 tolerance. Such cells are labeled \emph{exact-audited}, not as ANN
target successes.

\subsection{Methods and providers}

Table~\ref{tab:methods} summarizes the method families. The public method name
describes the search strategy; returned metadata records the provider and
resolved backend. Thus a request cannot silently move from CUDA to CPU. The
suffix \code{\_style} in \method{nsg\_style}, \method{vamana\_style}, and
\method{nndescent\_style} is a scope marker, not an algorithm name. The
package-owned CPU routes are distinct graph-refinement algorithms that borrow
selected construction or pruning ideas from canonical NSG, Vamana, or
NN-descent; they are not feature-complete reproductions. On CUDA,
\method{nndescent\_style} may instead resolve to the external cuVS NN-descent
implementation. Returned metadata distinguishes a package-owned derived
algorithm from a direct external-provider implementation.

The three exhaustive names are not three mathematical estimators and are not
simple aliases. They are distinct dispatch policies within one exhaustive
family: \method{exact} chooses the preferred available exhaustive provider,
\method{flat} explicitly requests a FAISS Flat index, and
\method{bruteforce} uses FAISS Flat on CPU but prefers direct cuVS brute force
on CUDA when available. More than one policy can therefore resolve to the same
provider on a particular installation. The requested name and concrete
\code{backend\_used} are both retained in result metadata.

\begin{table}[t]
\centering
\caption{Nearest-neighbor method families exposed by \pkg{faissR}. Availability
depends on the linked FAISS and CUDA/cuVS build.}
\label{tab:methods}
\begin{tabularx}{\textwidth}{@{}l l X@{}}
\toprule
Public method & Family & Principal controls \\
\midrule
\method{exact}, \method{flat}, \method{bruteforce} & Exhaustive & Query batch and resource reuse \\
\method{grid} & Exact low-dimensional & Spatial cell width and query expansion \\
\method{hnsw} & Navigable graph & Graph degree, construction and search effort \\
\method{ivf} & Inverted file & Number of lists and probed lists \\
\method{ivfpq} & IVF with product quantization & Lists, probes, subquantizers, code width \\
\method{ivfpq\_fastscan} & Fast accumulation IVF-PQ & IVF-PQ controls, block size, refinement \\
\method{cagra} & CUDA graph & Graph degrees and search width \\
\method{nndescent\_style} & Derived refinement or cuVS NN-descent & Candidate pool and refinement iterations \\
\method{nsg\_style}, \method{vamana\_style} & Package-owned derived refinements & Seed graph, degree and search breadth \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Data movement and fitted indexes}

Base \proglang{R} matrices are converted once to contiguous float32 storage at
the compiled boundary. Objects from \pkg{float} can bypass the double-to-float
conversion. Cosine and correlation transformations are cached when the same
data are reused. Query matrices are submitted in batches; returned identifiers
and distances are already ordered, and self-neighbor removal occurs in compiled
code.

The \fct{knn} interface builds a reusable model for classification or
regression, while \fct{predict} queries the fitted index. This distinction is
important for HNSW, IVF, and IVF-PQ because construction or training may
dominate one-call timing. On CUDA, \fct{nn\_gpu} returns an opaque
\class{faissR\_gpu\_knn} object whose neighbor buffers remain device-resident.
The explicit \fct{gpu\_knn\_to\_host} function performs transfer to ordinary
\proglang{R} matrices. Metadata include the requested and resolved method,
backend, input representation, result residency, and device-to-host copy count.
Predictive \fct{knn} and \fct{fast\_kmeans} are secondary convenience
interfaces in this article: they reuse the same compiled infrastructure, but
the empirical claims below concern nearest-neighbor search rather than
classification, regression, or clustering quality.

\section[The R interface]{The \proglang{R} interface} \label{sec:interface}

The central call separates algorithm from execution device:
\begin{CodeChunk}
\begin{CodeInput}
R> library("faissR")
R> fit <- nn(x, k = 30, exclude_self = TRUE,
+           backend = "cpu", method = "auto",
+           metric = "euclidean", tuning = "auto",
+           target_recall = 0.99)
R> c(attr(fit, "backend_used"), attr(fit, "input_type"))
\end{CodeInput}
\end{CodeChunk}
The package-wide backend can be set with \fct{faissR\_backend}, an
\proglang{R} option, or an environment variable. An explicit function argument
has highest precedence, and unsupported backends fail rather than falling back.

\subsection{A practical CPU workflow}

The following example uses the 20,000-row Letter Recognition data from
\pkg{mlbench} \citep{FreySlate1991,mlbench}. It compares automatic operating
points with exact neighbors and then reuses a fitted index for prediction.

\begin{CodeChunk}
\begin{CodeInput}
R> data("LetterRecognition", package = "mlbench")
R> y <- LetterRecognition$lettr
R> x <- scale(as.matrix(LetterRecognition[, -1L]))
R> exact <- nn(x, k = 15, exclude_self = TRUE,
+             method = "exact", metric = "euclidean")
R> recall_at_k <- function(reference, candidate) {
+   mean(vapply(seq_len(nrow(reference)), function(i)
+     length(intersect(reference[i, ], candidate[i, ])) /
+       ncol(reference), numeric(1)))
+ }
R> targets <- c(0.90, 0.95, 0.99)
R> observed <- vapply(targets, function(tau) {
+   z <- nn(x, k = 15, exclude_self = TRUE,
+           method = "auto", metric = "euclidean",
+           tuning = "auto", target_recall = tau)
+   recall_at_k(exact$indices, z$indices)
+ }, numeric(1))
\end{CodeInput}
\end{CodeChunk}

For train/test prediction, index construction is separated from query time:
\begin{CodeChunk}
\begin{CodeInput}
R> train <- seq_len(16000L)
R> model <- knn(x[train, ], y[train], k = 15,
+              method = "auto", metric = "euclidean",
+              tuning = "auto", target_recall = 0.99)
R> prediction <- predict(model, x[-train, ])
R> mean(prediction == y[-train])
R> attr(prediction, "faissR_nn")$query_source
[1] "fitted_index"
\end{CodeInput}
\end{CodeChunk}
The complete executable example records exact time, observed recall, selected
method, build time, first-query time, repeated-query time, and prediction
accuracy. It is included with the replication material.

\subsection{GPU-resident output}

When CUDA support is compiled, the nearest-neighbor result can remain on the
GPU:
\begin{CodeChunk}
\begin{CodeInput}
R> g <- nn_gpu(x, k = 30, exclude_self = TRUE,
+            method = "auto", metric = "euclidean",
+            tuning = "auto", target_recall = 0.99)
R> c(attr(g, "result_residency"),
+    attr(g, "device_to_host_result_copies"))
R> host_result <- gpu_knn_to_host(g)
\end{CodeInput}
\end{CodeChunk}
The opaque object is intended for downstream compiled packages; direct
inspection in \proglang{R} requires the explicit transfer.

\section{Recall-aware operating-point selection} \label{sec:tuning}

Let $s$ index candidate settings for a fixed dataset, method, metric, backend,
$k$, and target $\tau$. Calibration records one elapsed time $T_s$ and one mean
query recall $R_s$ from Equation~\ref{eq:recall} for each candidate. The feasible
set for approximate candidates is
\begin{equation}
\mathcal{F}_{\tau}=\{s: R_s\geq\tau,
\quad s\text{ completed without failure}\}.
\end{equation}
The selected setting minimizes $T_s$ over $\mathcal{F}_{\tau}$, with
deterministic tie-breaking by higher recall and candidate identifier.
Exact-audited candidates form a separate eligible class. If no approximate
candidate reaches $\tau$, metadata label the returned choice as below target;
this preserves the measured status of the selected operating point.

Method-specific grids cover search effort and representation parameters rather
than applying one generic knob. Examples include \code{ef\_search} for HNSW,
\code{nlist}/\code{nprobe} for IVF, product-quantizer dimensions for IVF-PQ,
and graph degree/search width for CAGRA. The resulting policy uses
$(n,p)$ shape class, metric, $k$, backend, target tier, and runtime capabilities.
Because data geometry and hardware are not fully described by these variables,
the selection metadata state whether a rule is directly measured or
extrapolated. On hardware unlike the calibration system, \method{auto} remains
available but should be treated as a calibration-informed default, not a
machine-specific optimum.

\section{Evaluation} \label{sec:evaluation}

\subsection{Datasets and protocol}

Nine real datasets span $873$ to $5.22$ million observations and $11$ to
$16,384$ variables (Table~\ref{tab:datasets}). COIL20 images were converted to
flattened $128\times128$ grayscale vectors \citep{Nene1996}; FashionMNIST and
MNIST combined their official training and test IDX files and flattened the
$28\times28$ pixels \citep{Xiao2017,LeCun1998}; and USPS used the 256-variable
matrix distributed with \pkg{Rdimtools} \citep{YouShung2022}. The
FR-FCM-ZYRM experiment retained 32 biological-marker channels from 102 FCS
files and applied $\operatorname{asinh}(x/5)$ \citep{VanUnen2017}. The flow18
and mass41 matrices used the supplied opt-SNE values without further
transformation \citep{Belkina2019}. ImageNet used 1,024-dimensional DINOv2
features from the ILSVRC 2012 training images, without additional scaling or
dimensionality reduction \citep{Russakovsky2015,Oquab2024}. MetRef removed
zero-sum variables and applied the documented KODAMA normalization and scaling
\citep{Cacciatore2014}. No labels entered neighbor timing or recall. All
analyses used float32 input; no benchmark dataset received PCA or another
dimension-reduction step.
For each real dataset, exact references were generated for 1,024 sampled
queries at $k=100$ and then cropped for $k\in\{15,30,50,100\}$. The targets were
$\{0.90,0.95,0.99\}$, with two validation seeds and three timing repeats.
Validation queries were sampled independently from calibration queries, but
both came from the same named datasets. We therefore distinguish this
query-replication experiment from a leave-one-dataset-out (LOODO) analysis. For
each LOODO cell, every row from the held-out named dataset was removed before a
method family was chosen from the remaining datasets. Selection first used the
same prespecified shape group and otherwise the three nearest training datasets
in $\log(n)$--$\log(p)$ space. The held-out dataset was used only to evaluate
target attainment, timing relative to its empirical oracle, method-family
agreement, abstention, and exhaustive-selection frequency.

\begin{table}[t]
\centering
\caption{Datasets used in the evaluation.}
\label{tab:datasets}
\begin{tabular}{@{}lrr@{}}
\toprule
Dataset & Rows & Variables \\
\midrule
COIL20 & 1,440 & 16,384 \\
FashionMNIST & 70,000 & 784 \\
flow18 & 1,000,021 & 11 \\
FR-FCM-ZYRM & 5,220,347 & 32 \\
ImageNet features & 1,281,167 & 1,024 \\
mass41 & 965,282 & 14 \\
MetRef & 873 & 375 \\
MNIST & 70,000 & 784 \\
USPS & 11,000 & 256 \\
\bottomrule
\end{tabular}
\end{table}

Route QA tested output dimensions, finite and sorted distances, backend
identity, metric invariances, degenerate rows, and GPU residency. Exact-family
methods were tie-audited. Calibration evaluated all available methods, while
the complete held-out CUDA comparison comprised \method{auto}, exhaustive
Flat, brute force, and CAGRA. The complete CPU comparison comprised
\pkg{faissR} HNSW and \pkg{BiocNeighbors} HNSW for Euclidean and cosine
distance \citep{BiocNeighbors}. The external route used the prespecified public
configuration \code{nlinks = 16}, \code{ef.construction = 200}, and
\code{ef.search = max(50, 3 * k)}. Correlation was evaluated for \pkg{faissR}
HNSW but was not available in the paired external comparison.

The package-level CPU \method{auto} route was not evaluated in the frozen
held-out campaign. Consequently, CPU results below characterize explicit HNSW
only, and no empirical CPU selector-generalization claim is made. CUDA LOODO
uses explicit held-out route measurements to evaluate a cross-fitted selector;
the installed package's compiled \method{auto} policy, which was calibrated on
the full collection, is retained only as a separate non-independent diagnostic.

Elapsed time includes public-interface transformations, index construction,
search, and output construction; it is therefore end-to-end API time. For a
matched cell $c$, the reported speed ratio is
\begin{equation}
  S_c = \frac{T_{c,\mathrm{second\ route}}}
              {T_{c,\mathrm{first\ route}}},
  \label{eq:speed-ratio}
\end{equation}
so $S_c>1$ favors the first route named in the comparison. Cells were matched
within dataset, metric, $k$, target, seed, and repeat when both routes
completed. Ratios were then summarized within dataset before the
across-dataset median and interquartile range were calculated. This preserves
the dataset as the primary unit and avoids pooling incomparable absolute
times.

The replacement primary CPU provider comparison is prespecified as controlled
experimental pairs. One Slurm array task fixes the dataset, $k$, and comparator,
and executes
\pkg{faissR} and the comparator within the same allocation and on the same
node. The initially executed route is selected reproducibly at random for each
validation seed and order alternates over five repetitions. Each route
repetition uses an isolated proglang{R} worker. Dataset and exact-reference
loading precede timing, followed by an untimed 128-row API warm-up; the
\pkg{faissR} fitted-index cache is then cleared. Cold one-shot time,
reusable-index construction time, and fitted-index query time are recorded
separately. Both routes receive the same double-precision matrix, while the
separate representation ablation measures the benefit of direct float32 input.
The primary audit rejects any pair whose allocation, node, or opposing order
cannot be verified. GPU kernels are synchronized before timing ends. The
supplement records the software environment and complete aggregation rules.

\subsection{Computing environment}

CPU jobs requested 12 threads on the heterogeneous UCT ``ada'' partition;
within-cell CPU ratios use only routes coexecuted on the same node. CUDA jobs
used one NVIDIA L40S (46,068 MiB,
compute capability 8.9) with driver 595.58.03. Both used \proglang{R} 4.5.3 on
Debian 13, OpenBLAS 0.3.33, and a frozen Singularity image. The evaluated
package release and full provider metadata are recorded in the supplement.

\section{Results} \label{sec:results}

\subsection{Contract and calibration evidence}

All 48 CPU and 52 CUDA route-contract cells passed. The exact-reference archive
contained all 87 records for the three public metrics and passed the identity
and CPU cross-audit. Across the three public metrics, calibration
produced 6,453 of 6,804 planned method--dataset--$k$--target operating points.
The 351 unavailable points comprised 318 CPU and 33 CUDA points: 306 reached the
48-hour limit and 45 were killed for memory exhaustion. They were concentrated
in FR-FCM-ZYRM (201), ImageNet features (144), and flow18 (6); the supplement
gives the method and metric breakdown. Of
4,725 approximate operating points, 3,653 (77.3\%) reached their requested
tier; the remaining 1,072 were retained as explicit negative evidence. A
further 1,728 exhaustive operating points were exact-audited. These results
show why method-specific feasibility must be checked rather than assumed.

Each successful candidate configuration was timed once, so replicate-to-replicate
winner changes cannot be estimated from this campaign. We therefore report
selection sensitivity rather than a replicate-stability percentage. Among
5,285 operating points with at least two eligible configurations, the
second-fastest configuration was within 5\% of the selected configuration in
60.8\% overall (43.6\% CPU and 75.8\% CUDA); competing method families were
within 5\% in 10.1\% of 641 comparable points. In independent-query validation,
median validation minus calibration recall was $+0.0053$ for CPU HNSW and
$0.0000$ for CUDA automatic selection. Median validation/calibration elapsed
ratios were 1.05 and 0.96, respectively; for the 44 CUDA IVF selections alone,
the corresponding values were $+0.0096$ recall and 2.12 time. Thus recall
generally transferred, while single-run calibration timing was less stable,
especially for CUDA IVF.

\subsection{Held-out recall}

Table~\ref{tab:heldout-recall} applies the replicate-level definition from
Section~\ref{sec:design}. CUDA \method{auto} passed all 324 cells. It selected
exact-audited Flat in 280 cells and IVF in 44 Euclidean cells; all 44 IVF cells
attained their target in every replicate. CPU HNSW passed 304 of 324 cells.
Most CPU misses occurred at the 0.99 tier, showing why stricter requested recall
tiers require provider- and parameter-aware operating-point selection.

\begin{table}[t]
\centering
\caption{Held-out target attainment. Exact-audited cells are eligible by the
tie-aware exhaustive audit; approximate cells must meet the mean-recall target
in every validation replicate.}
\label{tab:heldout-recall}
\begin{tabular}{@{}llrrr@{}}
\toprule
Backend and route & Metric & 0.90 & 0.95 & 0.99 \\
\midrule
CUDA automatic & Euclidean & 36/36 & 36/36 & 36/36 \\
CUDA automatic & Cosine & 36/36 & 36/36 & 36/36 \\
CUDA automatic & Correlation & 36/36 & 36/36 & 36/36 \\
CPU HNSW & Euclidean & 35/36 & 36/36 & 28/36 \\
CPU HNSW & Cosine & 35/36 & 36/36 & 32/36 \\
CPU HNSW & Correlation & 34/36 & 35/36 & 33/36 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Contribution of recall-aware automatic selection}

The route decomposition in Table~\ref{tab:auto-selection} qualifies the
aggregate 324/324 result. Flat was selected in 280 cells (86.4\%) and IVF in 44
(13.6\%). Cosine and correlation always used Flat. Within Euclidean cells, IVF
was selected in 44/108: 16 cells at each of targets 0.90 and 0.95 and 12 at
0.99. Each value of $k$ had 11 IVF selections, so the aggregate route frequency
did not vary with $k$.

\begin{table}[t]
\centering
\caption{CUDA automatic route selection by metric and requested recall. Each
row contains 36 dataset--$k$ cells.}
\label{tab:auto-selection}
\begin{tabular}{@{}lrrr@{}}
\toprule
Metric & Target & Flat & IVF \\
\midrule
Euclidean & 0.90 & 20 & 16 \\
Euclidean & 0.95 & 20 & 16 \\
Euclidean & 0.99 & 24 & 12 \\
Cosine & 0.90/0.95/0.99 & 36 at each target & 0 \\
Correlation & 0.90/0.95/0.99 & 36 at each target & 0 \\
\bottomrule
\end{tabular}
\end{table}

Target recall changed the selected method in 4/108 dataset--metric--$k$
strata: ImageNet features used IVF at 0.90 and 0.95 but Flat at 0.99 for every
$k$. It changed IVF's numeric \code{nlist}/\code{nprobe} setting without
changing method in another 6/108 strata: $k=15$ and $k=30$ for each of flow18,
FR-FCM-ZYRM, and mass41. Thus the target affected method or parameters in
10/108 strata (9.3\%); the other 98 were invariant across target tiers. This
shows that recall awareness contributes selectively for the large Euclidean
cases represented in calibration, while most tested cases conservatively
resolve to exhaustive search. Dataset-, $k$-, and parameter-level counts are
reported in the supplement and machine-readable analysis output.

\subsection{Leave-one-dataset-out selector sensitivity}

The CUDA LOODO analysis evaluated 324 cells spanning nine datasets, three
metrics, four values of $k$, and three target tiers. The cross-fitted selector
attained the requested operating point in 324/324 cells, never abstained, and
agreed with the held-out empirical oracle's method family in 300/324 cells
(92.6\%). It selected an exact-audited family in 288 cells (88.9\%) and CAGRA
in 36. The median cross-fitted/oracle elapsed-time ratio was 1.00 (90th
percentile 1.00; range 1.00--2.24). Thus, the main generalization result is not
that an approximate method always wins: on this collection, conservative
exhaustive selection was usually the reproducible choice.

Because LOODO removes dataset identity but still uses the other eight members
of the same benchmark collection, it evaluates sensitivity to a previously
unseen named dataset within this collection. It does not establish a universal
recall or speed guarantee for arbitrary data distributions or hardware.

\subsection{Matched-cell execution time}

Table~\ref{tab:paired-time} reports dataset-level paired CUDA ratios. CPU HNSW
provider ratios from separately submitted jobs are excluded because route and
node effects were not experimentally paired. The controlled same-node CPU
experiment described above is a required evidence gate: its cold-call,
reusable-index build, and fitted-query results will be reported only after the
allocation, order, completeness, and recall-equivalence audit passes. No CPU
provider speedup is claimed from the analytically matched archive.

On CUDA, automatic selection and explicit exhaustive routes had similar timing
because 280 of 324 cells resolved to Flat. The selector carried modest dispatch
overhead relative to explicit exact or brute-force calls. Against CAGRA, the
median dataset-level ratio favored automatic selection by 2.23. In the 44 cells
where the selector used IVF, its median dataset-level speedup over exhaustive
search was 4.59 across four datasets, but ImageNet features were a clear
counterexample: explicit exhaustive search was 7.36 times faster. This
heterogeneity is precisely why the selected method and calibration evidence are
returned to the user. Supplementary Table~5 reports all nine datasets. The
IVF-selected result is descriptive evidence from only four datasets:
FR-FCM-ZYRM, flow18, and mass41 favored IVF by factors of 3.69, 5.83, and 5.49,
whereas ImageNet favored exhaustive search. It should not be interpreted as a
four-dataset estimate of performance on a wider population.

\begin{table}[t]
\centering
\caption{Matched-cell CUDA end-to-end speed ratios, defined as
$T_{\mathrm{second\ route}}/T_{\mathrm{first\ route}}$. Values greater than one
favor the first route. Ratios are summarized within dataset and then across
datasets.}
\label{tab:paired-time}
\begin{tabularx}{\textwidth}{@{}Xlrrr@{}}
\toprule
Comparison & Datasets & Median & IQR & Range \\
\midrule
CUDA automatic / explicit exact & 9 & 0.99 & 0.98--1.00 & 0.94--1.01 \\
CUDA automatic / brute force & 9 & 0.87 & 0.83--1.18 & 0.70--1.90 \\
CUDA automatic / CAGRA & 9 & 2.23 & 1.00--2.58 & 0.81--10.96 \\
CUDA IVF-selected cells / explicit exact & 4 & 4.59 & 2.80--5.58 & 0.14--5.83 \\
\bottomrule
\end{tabularx}
\end{table}

\section{Limitations} \label{sec:limitations}

The calibration policy is empirical. Shape descriptors do not capture
intrinsic dimension, clustering, ties, or anisotropy, and timing depends on
hardware and provider versions. Same-dataset query replication and CUDA LOODO
address different questions: the former tests repeated queries, while the
latter tests sensitivity to withholding one named dataset from method-family
selection. Neither establishes cross-collection generalization. CPU
\method{auto} was not independently evaluated, so selector claims are limited
to CUDA; CPU results concern explicit HNSW. The CPU partition is
heterogeneous, and the CUDA results describe an NVIDIA L40S rather than all
accelerators. CPU provider speed ratios require completion of the controlled
same-node experiment and are not inferred from the separate-job archive.
GPU-resident output reduces transfer only when downstream code
can consume the opaque device buffers. Finally, the completed external
comparison evidence remains incomplete. The controlled HNSW experiment covers
\pkg{BiocNeighbors} and \pkg{RcppHNSW}; broader FNN and \pkg{rnndescent}
comparisons require their prespecified focused experiments before numerical
claims are added.

These limits motivate conservative use: inspect \code{auto\_selection} and
\code{approximation} metadata, validate recall against an exact subsample when
the application is sensitive, and fit a persistent index when queries repeat.

\section{Discussion and conclusion} \label{sec:conclusion}

\pkg{faissR} provides a single nearest-neighbor interface across CPU and
NVIDIA GPU providers while preserving the distinctions that matter for
statistical computing: metric semantics, exact versus approximate evidence,
data representation, backend identity, and result residency. Its recall-aware
policies make the speed--accuracy choice visible and reproducible rather than
embedding it in undocumented defaults.

The evaluation supports three conclusions. First, the common route and metric
contract is implemented consistently across the tested CPU and CUDA paths.
Second, many approximate operating points do not reach ambitious recall tiers,
so negative calibration evidence is scientifically useful. Third, automatic
selection can choose a reliable and often efficient CUDA route, but its
hardware- and dataset-dependent exceptions must remain visible. The package is
therefore best understood as auditable computational infrastructure, not as a
universal claim that one nearest-neighbor method dominates.

\section*{Data and code availability}

Package source, documentation, tests, manuscript source, and replication code
are available at \url{https://github.com/tkcaccia/faissR}. The replication
bundle includes a frozen result archive and SHA-256 ledger; its standalone
script verifies the archive before extraction and recreates every article and
supplementary table with a machine-readable table manifest. A reduced CPU mode
runs the interface examples without CUDA. Dataset acquisition
and preprocessing scripts record source identifiers and fingerprints. Data
whose redistribution terms do not permit inclusion are obtained from their
original sources by the replication script; no substitute datasets are used.

\section*{Acknowledgments}

The authors thank the University of Cape Town's ICTS High Performance
Computing team for providing a high-performance computing facility for this
study (\url{https://ucthpc.uct.ac.za/}). We also acknowledge the developers and
communities of FAISS, RAPIDS cuVS, CUDA, \proglang{R}, \pkg{Rcpp},
\pkg{float}, Singularity, and Bioconductor.

\bibliography{faissR_jss}

\end{document}
